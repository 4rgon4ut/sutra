runtime: "container"
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
startCommand:
  type: "stdio"
  config:
    command: "python"
    args: ["-m", "context_engineering_mcp.server"]
  configSchema:
    type: "object"
    properties: {}